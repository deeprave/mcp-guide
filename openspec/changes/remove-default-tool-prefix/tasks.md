## 1. Implementation
- [ ] 1.1 `tool_decorator.py` — change `get_tool_prefix()` default from `"guide"` to `""`
- [ ] 1.2 `cli.py` — change `ServerConfig.tool_prefix` default from `"guide"` to `""`; remove `--no-tool-prefix` flag and its handling; update `--tool-prefix` help text
