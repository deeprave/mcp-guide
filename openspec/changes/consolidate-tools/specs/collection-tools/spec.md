## REMOVED Requirements

### Requirement: collection_list Tool
**Reason**: Consolidated into `category_collection_list` tool
**Migration**: Use `category_collection_list(type="collection", verbose=...)` instead

### Requirement: collection_remove Tool
**Reason**: Consolidated into `category_collection_remove` tool
**Migration**: Use `category_collection_remove(type="collection", name=...)` instead
