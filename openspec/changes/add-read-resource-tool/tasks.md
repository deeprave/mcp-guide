## 1. Implementation

- [ ] 1.1 Add `read_resource` tool accepting a `guide://` URI string parameter
- [ ] 1.2 Parse the URI into collection and optional document components
- [ ] 1.3 Delegate to existing `internal_get_content` with mapped parameters
- [ ] 1.4 Return content as Result with appropriate error handling
- [ ] 1.5 Add tests for valid URIs, missing content, malformed URIs, and scheme validation
