## 1. Audit existing disposition, instruction, and additional_agent_instructions usage

- [ ] 1.1 Enumerate all `Result.failure()`/`Result.ok()` construction sites (expect ~229 across ~26 files per this session's grep) and record, per site: current `disposition` (if any), current `instruction` text, error_type/success context, and whether the instruction is paired-with-type (drawn from a `result_constants.py` constant) or ad hoc. Verify completion by producing a table/list covering every site found by `grep -rn "Result\.failure(\|Result\.ok("`.
- [ ] 1.2 Enumerate all `additional_agent_instructions` usage sites (direct string assignments and rendered-template sources under `src/mcp_guide/templates/`), and for each record: source (inline string vs. template path), current tone (imperative vs. already cooperative), and a one-line note on whether disposition alone looks sufficient once the vocabulary is taught. Do not edit any of this content. Verify completion by a list covering every site found by `grep -rln "additional_agent_instructions"` in both `src/mcp_guide` and `src/mcp_guide/templates`.
- [ ] 1.3 Read `combine_instructions` (`src/mcp_guide/content/utils.py`) and its callers (including `render/content.py`'s `RenderedContent.instruction` property) and confirm or correct the design.md description of important/regular priority-replacement and deduplication behavior against the actual code and its existing tests. Verify completion by updating design.md's Context section if the behavior differs from what is currently documented there, and by citing the specific test(s) that exercise this behavior.
- [ ] 1.4 Identify every place a `Result` can be returned before `StartupInstructionListener` has had a chance to queue the disposition-guide template for that session (e.g. very early tool-decorator failures such as `_check_project_bound`, invalid-session handling in `tool_decorator.py`). Verify completion by a short list of such call sites, used to decide in task 3 which pilot sites need a fallback instruction versus disposition alone.

## 2. Define vocabulary and teaching mechanism

- [x] 2.0 Remove dead `AGENT_REQUIREMENTS`/`INSTRUCTION_AGENT_REQUIREMENTS` constants (confirmed zero live usages by audit) from `result_constants.py`, `render/frontmatter.py`'s `get_default_instruction_for_type()` lookup table, and `render/cache.py`'s `_build_system_context` template-context export. Done ahead of the rest of this task group; verified by `grep -rn "AGENT_REQUIREMENTS"` returning no matches and the full test suite passing.
- [ ] 2.1 Add `AGENT_ERROR = "agent/error"` and `USER_ERROR = "user/error"` constants to `result_constants.py`, alongside the existing three disposition constants. Verify completion by a passing unit test asserting both constants' literal values match the spec.
- [ ] 2.2 Create `src/mcp_guide/templates/_system/_disposition-guide.mustache` teaching all five dispositions (`user/information`, `agent/information`, `agent/instruction`, `agent/error`, `user/error`) in the terse, second-person, why/what/action style validated this session in `_filesystem-probe.mustache` and `_project-root.mustache`, gated by a new `requires-disposition-guide` frontmatter flag. Verify completion by rendering the template directly in a unit test and asserting all five disposition strings appear in the output.
- [ ] 2.3 Wire `_disposition-guide` into `StartupInstructionListener._render_and_queue`, queued alongside `_startup` and `_onboard_prompt`, gated by its `requires-*` flag the same way the existing two are. Verify completion by a test asserting the instruction is queued once per session on `on_project_changed` and not re-queued on subsequent calls within the same session.
- [ ] 2.4 Update `get_default_instruction_for_type()` (`render/frontmatter.py`) so `USER_INFO`, `AGENT_INFO`, and `AGENT_INSTRUCTION` return `None` instead of their current paired imperative constants, once the disposition-guide template exists to carry that meaning instead. Verify completion by updating the existing unit test(s) covering this function and confirming they assert `None` for these three types.
- [ ] 2.5 Retire `INSTRUCTION_AGENT_INFORMATION`, `INSTRUCTION_AGENT_INSTRUCTIONS`, and `INSTRUCTION_DISPLAY_ONLY` from `result_constants.py` if task 2.4 leaves them with no remaining callers; otherwise leave them and note remaining callers for the follow-up migration. Verify completion by `grep -rn` confirming each retired constant has zero references, or by a written note listing remaining callers if not fully retired.

## 3. Pilot migration

- [ ] 3.1 Rewrite `make_no_project_result`, `make_invalid_session_result`, and `make_unmintable_session_result` in `result_constants.py` to set the appropriate disposition (`agent/error` for a self-correctable no-project/no-session state) and drop or shorten their paired prose instruction per task 1.4's findings (keep a minimal fallback instruction only where the disposition-guide cannot yet be assumed queued). Verify completion by the existing `tests/unit/test_no_project_result.py` (updated as needed) plus new/updated tests for the other two helpers asserting the new disposition value.
- [ ] 3.2 Select two or three tool-layer call sites with the highest fan-out from task 1.1's audit and migrate them to set disposition and a cooperative, response-specific instruction (only where the disposition alone is insufficient). Verify completion by updated tests at each migrated site asserting the new disposition and instruction content.
- [ ] 3.3 Run the full test suite and confirm no regressions outside the migrated sites and their direct consumers. Verify completion by a full `pytest` run reporting 0 unexpected failures (compare against the pre-change baseline pass count).

## 4. Documentation and follow-up tracking

- [x] 4.0 Amend `openspec/adr/003-result-pattern-response.md` with a dated addendum noting that disposition supersedes the original "Instruction Field Semantics" imperative-pattern catalog for new code, following the file's existing 2025-12-07 amendment precedent (append, do not rewrite); update its `openspec/adr/INDEX.md` entry to note the amendment. Done ahead of the rest of this task group; verified by `openspec validate` still passing and the original ADR text remaining intact below the addendum.
- [ ] 4.1 Record task 1.2's `additional_agent_instructions` inventory as a follow-up item for the `update-developer-docs` change (created as a stub alongside this change) rather than acting on it here, per proposal scope. Verify completion by a written inventory artifact that `update-developer-docs` can start from without re-auditing.
- [ ] 4.2 Record the ~220 remaining `Result` construction sites from task 1.1 not covered by the pilot as follow-up migration work for the `update-openspec-specs` change (created as a stub alongside this change). Verify completion by a written list or tracked item enumerating the remaining sites grouped by file.
- [ ] 4.3 Confirm this change's disposition rewiring does not break `prompt-infrastructure`'s "preserving rendered instruction dispositions" requirement through the prompt result adapter. Verify completion by running the existing tests covering that requirement and confirming they still pass unmodified.
