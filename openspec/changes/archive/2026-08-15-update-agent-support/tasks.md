## 1. Capability model

- [x] 1.1 Make absent prompt support the default; set Cursor Agent and Pi prompt support to absent; canonicalise Pi client aliases, including `pi-mcp-guide`, to `pi`

## 2. Verification

- [x] 2.1 Add agent detection tests for Cursor Agent, Pi, the `pi-mcp-guide` alias, and the no-prompt default
