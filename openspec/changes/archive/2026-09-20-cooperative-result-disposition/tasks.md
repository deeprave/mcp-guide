## 1. Audit existing disposition, instruction, and additional_agent_instructions usage

- [x] 1.1 Enumerate all `Result.failure()`/`Result.ok()` construction sites (expect ~229 across ~26 files per this session's grep) and record, per site: current `disposition` (if any), current `instruction` text, error_type/success context, and whether the instruction is paired-with-type (drawn from a `result_constants.py` constant) or ad hoc. Verify completion by producing a table/list covering every site found by `grep -rn "Result\.failure(\|Result\.ok("`. **Done** — full audit written to `.todo/result-disposition-audit.md` (707 lines): 229 grep matches, 227 real construction sites (2 are docstring examples in `tool_result.py`, not live code). Top fan-out files: `tool_project.py` (34), `tool_feature_flags.py` (26), `guide_prompt.py` (26), `tool_category.py` (23, pairs with `tool_collection.py`'s 12). Also flagged: the 7 pre-existing `disposition=`-bearing sites, a repeated ad hoc `"max_size_exceeded"` error_type, a likely-buggy swapped-argument call in `tool_project.py` (lines 788/800, left unfixed per read-only audit scope, worth a separate look), and the most-reused instruction/error_type pairing (`INSTRUCTION_NOTFOUND_ERROR`+`ERROR_NOT_FOUND`, 10 sites/4 files).
- [x] 1.2 Enumerate all `additional_agent_instructions` usage sites (direct string assignments and rendered-template sources under `src/mcp_guide/templates/`), and for each record: source (inline string vs. template path), current tone (imperative vs. already cooperative), and a one-line note on whether disposition alone looks sufficient once the vocabulary is taught. Do not edit any of this content. Verify completion by a list covering every site found by `grep -rln "additional_agent_instructions"` in both `src/mcp_guide` and `src/mcp_guide/templates`. **Done** — the field has no direct template references; it is populated generically by `task_manager/manager.py:1113` from whatever instruction is queued. Traced all 12 `queue_instruction(_with_ack)` call sites to their 11 source templates and recorded tone/priority in `.todo/additional-agent-instructions-audit.md`. Flagged `_context/client-context-detailed.mustache` as the highest-priority rewrite candidate (most invasive data collection, zero stated rationale).
- [x] 1.3 Read `combine_instructions` (`src/mcp_guide/content/utils.py`) and its callers (including `render/content.py`'s `RenderedContent.instruction` property) and confirm or correct the design.md description of important/regular priority-replacement and deduplication behavior against the actual code and its existing tests. Verify completion by updating design.md's Context section if the behavior differs from what is currently documented there, and by citing the specific test(s) that exercise this behavior. **Done** — confirmed accurate against `tests/test_partial_instruction_merging.py::test_partial_instruction_merging`, parametrized case `"important-overrides-all"` (an important child instruction drops both the parent's and the other (regular) child's instructions entirely, not merged) and `"fuzzy-deduplication"` (near-identical sentences differing only in trailing punctuation collapse to one). design.md's Context section already stated this correctly; no correction needed.
- [x] 1.4 Identify every place a `Result` can be returned before `StartupInstructionListener` has had a chance to queue the disposition-guide template for that session (e.g. very early tool-decorator failures such as `_check_project_bound`, invalid-session handling in `tool_decorator.py`). Verify completion by a short list of such call sites, used to decide in task 3 which pilot sites need a fallback instruction versus disposition alone. **Done** — `StartupInstructionListener.on_project_changed` (`startup_listener.py:22`) only fires once a project successfully *binds*; it cannot have run for any session that is still unbound or that failed to mint at all. Confirmed all three pilot helper functions fall exactly in this window: `make_no_project_result` (`tool_decorator.py:98`, unbound session — binding never happened), `make_invalid_session_result` (`tool_decorator.py:210`, `prompt_decorator.py:111`, invalid/expired session ID — no session to attach a listener to), `make_unmintable_session_result` (`tool_decorator.py:212`, protocol cannot carry a session at all). Conclusion for task 3: all three pilot sites need a retained fallback instruction string (not disposition alone), since none can assume the disposition-guide was queued for that session.

## 2. Define vocabulary and teaching mechanism

- [x] 2.0 Remove dead `AGENT_REQUIREMENTS`/`INSTRUCTION_AGENT_REQUIREMENTS` constants (confirmed zero live usages by audit) from `result_constants.py`, `render/frontmatter.py`'s `get_default_instruction_for_type()` lookup table, and `render/cache.py`'s `_build_system_context` template-context export. Done ahead of the rest of this task group; verified by `grep -rn "AGENT_REQUIREMENTS"` returning no matches and the full test suite passing.
- [x] 2.1 Add `AGENT_ERROR = "agent/error"` and `USER_ERROR = "user/error"` constants to `result_constants.py`, alongside the existing three disposition constants. Verify completion by a passing unit test asserting both constants' literal values match the spec. **Done** — added to `result_constants.py`; new `tests/unit/test_result_constants.py::test_error_disposition_constants_match_spec` passes.
- [x] 2.2 Deliver disposition guidance as part of startup. **Done**: a reusable partial is included by `_startup.mustache`, so it is delivered through the existing startup path. Coverage verifies startup instruction delivery behaviour without asserting user-editable template wording.
- [x] 2.3 Wire `_disposition-guide` into `StartupInstructionListener._render_and_queue`, queued alongside `_startup` and `_onboard_prompt`, gated by its `requires-*` flag the same way the existing two are. Verify completion by a test asserting the instruction is queued once per session on `on_project_changed` and not re-queued on subsequent calls within the same session. **Superseded by 2.2's revised approach — already satisfied, nothing further to wire**: since the disposition guide is a partial included directly by `_startup.mustache` (`includes: - _partials/disposition-guide`, `{{>disposition-guide}}`), it is delivered every time `_startup` is queued — which `StartupInstructionListener._render_and_queue` already does, unmodified, on `on_project_changed`. No separate queuing, gating, or wiring was needed; the once-per-session guarantee is inherited from `_startup`'s own existing behavior, not built anew.
- [x] 2.4 Update `get_default_instruction_for_type()` (`render/frontmatter.py`) so `USER_INFO`, `AGENT_INFO`, and `AGENT_INSTRUCTION` return `None` instead of their current paired imperative constants, once the disposition-guide template exists to carry that meaning instead. Verify completion by updating the existing unit test(s) covering this function and confirming they assert `None` for these three types. **Done, expanded in scope per user direction mid-implementation**: `get_default_instruction_for_type` now always returns `None` (the per-type lookup table it used is gone — the disposition itself, once taught, is self-sufficient for every recognized type; an unrecognized type also gets no fabricated instruction). The application-level blanket default mechanism was found and removed too: `Result.default_success_instruction`/`default_failure_instruction` class vars and their `success_instruction()`/`failure_instruction()` classmethods (`core/result.py`) are deleted; `mcp_guide/result.py` no longer sets any default instruction or disposition at import time — a `Result` with no disposition/instruction now genuinely has none, correctly omitted from `to_json()`, rather than being defaulted to a generic value regardless of content. A parallel attempt to add `default_success_disposition`/`default_failure_disposition` class vars was tried and explicitly reverted per user correction: disposition must stay situational (set only where content genuinely has a known one — a rendered document/skill/command), never blanket-defaulted. Verified: per-category defaults already work correctly via existing code with no changes needed — skills/commands default to `agent/instruction` via `RenderedContent.__post_init__`'s construction-time default (confirmed live against this session's own `guide://$workflow-implement` skill call); documents default to `user/information` via the existing `resolve_content_properties()` → `.with_disposition_default(USER_INFO)` pattern already used by both `tool_content.py` and `tool_category.py`. Also removed the now-fully-unused `startup-instruction` feature flag (`FLAG_STARTUP_INSTRUCTION`, its validator, and its `TestStartupInstructionValidation` test class) and rewrote `_startup.mustache` to unconditionally teach the disposition vocabulary (previously gated behind that flag, which had no default-on mechanism and would never have fired). Full test suite: 1924 passed after all fixes.
- [x] 2.5 Retire `INSTRUCTION_AGENT_INFORMATION`, `INSTRUCTION_AGENT_INSTRUCTIONS`, and `INSTRUCTION_DISPLAY_ONLY` from `result_constants.py` if task 2.4 leaves them with no remaining callers; otherwise leave them and note remaining callers for the follow-up migration. Verify completion by `grep -rn` confirming each retired constant has zero references, or by a written note listing remaining callers if not fully retired. **Done — not retired, real remaining callers found**: `INSTRUCTION_DISPLAY_ONLY` is used as an explicit ad hoc `instruction=` value (not via the removed default mechanism) in `tools/tool_resource.py` and multiple sites in `tools/tool_feature_flags.py`; `INSTRUCTION_AGENT_INFORMATION` is used explicitly in `tools/tool_resource.py`'s `list_skills`. These are legitimate per-call-site uses independent of the retired default-instruction mechanism and are left in place for the ~220 non-pilot `Result` construction sites tracked in `.todo/result-disposition-audit.md`, rather than touched here.

## 3. Pilot migration

- [x] 3.1 Rewrite `make_no_project_result`, `make_invalid_session_result`, and `make_unmintable_session_result` in `result_constants.py` to set the appropriate disposition (`agent/error` for a self-correctable no-project/no-session state) and drop or shorten their paired prose instruction per task 1.4's findings (keep a minimal fallback instruction only where the disposition-guide cannot yet be assumed queued). Verify completion by the existing `tests/unit/test_no_project_result.py` (updated as needed) plus new/updated tests for the other two helpers asserting the new disposition value. **Done** — split by who can act on it, not by severity: `make_no_project_result` and `make_invalid_session_result` are agent-fixable (call `set_project`, or discard the session ID and retry) → `AGENT_ERROR`; `make_unmintable_session_result` is a client-protocol constraint the agent cannot fix → `USER_ERROR`, and its instruction's old "Present this error to the user" prefix was dropped since `user/error` now says that on its own. All three keep a static/rendered fallback instruction per task 1.4 (none can assume `_disposition-guide` was queued pre-bind). Updated `tests/unit/test_no_project_result.py` (both cases) and `tests/unit/test_mcp_core/test_tool_decorator.py` (both helpers) to assert the new `disposition` field; 4 targeted tests pass.
- [x] 3.2 Select two or three tool-layer call sites with the highest fan-out from task 1.1's audit and migrate them to set disposition and a cooperative, response-specific instruction (only where the disposition alone is insufficient). Verify completion by updated tests at each migrated site asserting the new disposition and instruction content. **Done, two files migrated**: `tool_feature_flags.py` (9 failure sites — the "easiest single-pattern sweep" candidate per the audit ranking) and `prompts/guide_prompt.py` (9 of its 26 sites — the constant-paired and ad hoc validation/security sites, deliberately excluding the 17 bare `error_type`-only sites and 2 passthrough sites as out of this pilot's scope). Applied the ownership split settled mid-implementation: `agent/error` for anything the agent caused or can fix by retrying/editing (bad flag name/value, empty command name, unsafe path, malformed flags, missing command, template syntax errors — the agent can edit the broken template); `user/error` for environment/permission failures the agent can't fix by retrying (`OSError` on config read/write, filesystem `PermissionError`); `unknown/error` for genuinely unclassified bare-`Exception` catches. Dropped now-redundant paired instructions (`INSTRUCTION_VALIDATION_ERROR`, `INSTRUCTION_DISPLAY_ONLY`, `INSTRUCTION_NOTFOUND_ERROR`, `INSTRUCTION_FILE_ERROR`) at migrated sites only — left the constants themselves in place since they retain real callers elsewhere (part of the non-pilot sites tracked in `.todo/result-disposition-audit.md`), except `INSTRUCTION_TEMPLATE_ERROR`, retired outright since its only caller was migrated. **Scope correction mid-task**: the `RuntimeError`/template-syntax site in `guide_prompt.py` turned out to be bound by a live `openspec/specs/template-support/spec.md` requirement pinning the literal instruction text — a site this change's own `proposal.md` had explicitly flagged as out of scope. Per user direction, brought it into scope properly: added a `specs/template-support/spec.md` MODIFIED-requirements delta (rewriting the "Agent error instruction" scenario to require `disposition: agent/error` instead of the literal string) and updated `proposal.md`'s Modified Capabilities list accordingly; `openspec validate --strict` passes. Added disposition assertions across `tests/unit/test_feature_flag_tools.py`, `tests/integration/test_feature_flag_tools.py`, `tests/integration/test_guide_prompt.py` (including fixing one test whose `"disposition" not in result` assertion predated this rollout and one new precise per-case assertion), and `tests/test_command_security.py` (2 new tests reaching previously-uncovered `RuntimeError`/bare-`Exception` branches via `render_template` mocking). Also found, while writing the flag-parsing disposition test for `_handle_content_request`'s migrated site, a genuine pre-existing bug unrelated to disposition: it passed a filtered `flags` list straight to `parse_command_arguments`, which assumes `argv[0]` is a command name to skip, silently dropping the *first* flag on every `@guide <category> --flag` content-mode request (confirmed: a lone `--force` was silently ignored; a second flag after it was correctly parsed). Fixed per user direction by passing `argv[0]` ("guide") ahead of the flags list, matching the convention `handle_command`'s command-path call already used correctly — commands (`:help --flag` etc.) were never affected, only bare content lookups. Added 2 regression tests (`test_content_request_rejects_a_single_malformed_flag`, `test_content_request_parses_a_single_valid_flag`) to `tests/integration/test_guide_prompt.py`. **Verification**: full suite 1929 passed, 0 failed after all of the above (up from the 1923-passing baseline recorded after the unrelated `asyncio.to_thread` removal), run with no concurrent file edits per the earlier thread-warning investigation's lesson.

## 4. Documentation and follow-up tracking

- [x] 4.0 Amend `openspec/adr/003-result-pattern-response.md` with a dated addendum noting that disposition supersedes the original "Instruction Field Semantics" imperative-pattern catalog for new code, following the file's existing 2025-12-07 amendment precedent (append, do not rewrite); update its `openspec/adr/INDEX.md` entry to note the amendment. Done ahead of the rest of this task group; verified by `openspec validate` still passing and the original ADR text remaining intact below the addendum.
- [x] 4.1 Confirm this change's disposition rewiring does not break `prompt-infrastructure`'s "preserving rendered instruction dispositions" requirement through the prompt result adapter. Verify completion by running the existing tests covering that requirement and confirming they still pass unmodified. **Done** — the requirement ("Modern Prompt Result Adaptation", `openspec/specs/prompt-infrastructure/spec.md:181`) is exercised unmodified by `tests/unit/test_mcp_surface_adaptation.py::test_tool_and_prompt_boundaries_preserve_agent_directed_result_fields`, which round-trips a synthetic `Result` carrying `disposition="agent/instruction"` plus `instruction` and `additional_agent_instructions` simultaneously through both `tool_result()` and `prompt_result()` and asserts all three survive unchanged. The adapter passes `Result` fields through generically rather than special-casing disposition values, so this change's new `agent/error`/`user/error`/`unknown/error` values and the removal of paired default instructions don't affect it. 30 tests pass across this file and `tests/integration/test_guide_prompt.py`, unmodified by this change.

**Removed (structural correction):** this section previously included tasks 4.1/4.2 directing this change's tasks.md to prescribe follow-up content for `update-developer-docs` and `update-openspec-specs` — two separate, independently-owned OpenSpec changes. A change's tasks.md SHALL only track its own implementation steps, never dictate scope or content for another change's proposal/design/tasks. This change's own output (the `.todo/additional-agent-instructions-audit.md` and `.todo/result-disposition-audit.md` artifacts from tasks 1.1/1.2) already exists and stands on its own; whether and how those other changes use it is entirely for their own planning to decide.
