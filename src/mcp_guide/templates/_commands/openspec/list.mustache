---
type: agent/instruction
description: List all OpenSpec changes in the project
category: "openspec"
usage: "guide://_openspec/list"
requires-openspec: true
includes:
  - ../../_openspec/list-format
---
{{#openspec}}
{{#kwargs.force}}
List `openspec/changes` and send it using {{tool_prefix}}send_directory_listing with path `openspec/changes`, its entries, and the directory's `mtime`.

Then run `openspec list --json` and send the result using {{tool_prefix}}send_file_content with path `.openspec-changes.json` to refresh the cache, then re-render this template to display the results.
{{/kwargs.force}}
{{^kwargs.force}}
{{#changes}}
{{>list-format}}
{{/changes}}

{{^changes}}
First list `openspec/changes` and send it using {{tool_prefix}}send_directory_listing with path `openspec/changes`, its entries, and the directory's `mtime`.

Then run `openspec list --json` and send the result using {{tool_prefix}}send_file_content with path `.openspec-changes.json` to refresh the cache, then re-render this template to display the results.
{{/changes}}
{{/kwargs.force}}
{{/openspec}}
