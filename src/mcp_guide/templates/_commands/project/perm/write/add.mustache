---
type: agent/instruction
description: Add path to allowed write paths
usage: "guide://_project/perm/write/add/<path>"
category: project-permissions
aliases:
  - perm/write/add
instruction: "{{INSTRUCTION_AGENT_INSTRUCTIONS}}"
---
{{#args.0.value}}
Use the add_permission_path tool with:
permission_type: 'write'
path: '{{args.0.value}}'
{{/args.0.value}}
{{^args.0.value}}
{{#_error}}Missing required argument: path{{/_error}}

{{b}}Usage:{{b}} `{{#command}}project/perm/write/add{{/command}} <path>`

{{b}}Example:{{b}} `{{#command}}project/perm/write/add{{/command}}{{#command-args}}docs/{{/command-args}}`

{{b}}Note:{{b}} Write paths must be relative to project root. Use trailing `/` for directories (e.g., `docs/`), omit for files (e.g., `config.json`).
{{/args.0.value}}
