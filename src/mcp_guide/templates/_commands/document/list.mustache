---
type: agent/instruction
description: List stored documents in a category
usage: "guide://_document/list/<category>"
category: project
minargs: 1
instruction: "{{INSTRUCTION_AGENT_INSTRUCTIONS}}"
---
List Stored Documents

Call the `{{tool_prefix}}category_list_files` tool:

```
{{tool_prefix}}category_list_files(
    category="{{args.0.value}}",
    source="stored"
)
```
