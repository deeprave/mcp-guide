---
type: agent/instruction
Description: Review project changes
usage: ":workflow/review"
aliases:
  - review
category: "project-workflow"
instruction: Perform a review of project changes. {{INSTRUCTION_AGENT_INSTRUCTIONS}}
---
{{#workflow}}You MUST transition to REVIEW phase.
{{/workflow}}
{{h1}}Perform a Code Review

{{#agent.class}}
{{#equals}}kiro{{agent.class}}
  — Use `use_subagent` to delegate review to `guide-review` agent with the instructions below.
{{/equals}}
{{#equals}}q-dev{{agent.class}}
  — Use `use_subagent` to delegate review to `guide-review` agent with the instructions below.
{{/equals}}

{{b}}Review Instructions:{{b}}
  — You MUST NOT consider previous conversational history when conducting this review.
    {{b}}This review must be done with a fresh look without consideration of previous work, including any fixes and tests.{{b}}
  — Use {{tool_prefix}}get_content("code-review") to access code review guidelines.
  — Read {{workflow.file}} to understand the current issue context {{#workflow.issue}}(issue: {{workflow.issue}}).{{/workflow.issue}}
  — Analyse changes against HEAD and create or update review artifact in `{{#workflow.issue}}.todo/{{workflow.issue}}-review.md{{/workflow.issue}}{{^workflow.issue}}.todo{{/workflow.issue}}`

{{#openspec}}{{b}}NOTE{{b}} Review artifacts are NEVER to be created in the openspec changes directory{{/openspec}}
{{/agent.class}}
{{^agent.class}}
{{b}}Agent Detection Required{{b}}
  — Run {{tool_prefix}}client_info() to detect the agent information
  — Report the result to the user
  — Ask the user to run this command again after agent detection completes

{{/agent.class}}
