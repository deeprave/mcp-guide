---
type: agent/instruction
description: >
  Git Policy: Conservative (Read-Only). The agent may use read-only git operations but must not modify the index, working tree, or history unless explicitly requested by the user.
---
# Git Policy: Conservative (Read-Only)

The agent may use read-only git operations but must not modify the index, working
tree, or history UNLESS EXPLICITLY REQUESTED BY THE USER.

## Permitted

- `git status`, `git diff`, `git log`, `git show`
- `git branch --list`, `git tag --list`
- `git stash list` (read only)

## Forbidden Unless Explicitly Requested

- `git add` — staging changes
- `git commit` — creating commits
- `git push` — updating remote branches
- `git reset`, `git restore`, `git checkout`, `git switch` — modifying working tree or HEAD
- `git rebase`, `git merge`, `git stash push/pop` — rewriting or modifying history

## Git MCP

If a git MCP server is connected, its tools are subject to the same permissions
as the equivalent CLI operations. Read-only tools (status, diff, log) are
permitted; tools that stage, commit, push, or otherwise modify state are
forbidden unless explicitly requested.

## Rationale

Useful when the agent needs to inspect repository state (e.g. check what has
changed before reviewing), but the user still owns all write operations.
