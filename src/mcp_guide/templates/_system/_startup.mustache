---
type: agent/instruction
requires-startup-instruction: true
instruction: >
  Execute startup initialization immediately.
  {{INSTRUCTION_NO_DISPLAY}}
---
**IMPORTANT**: You MUST IMMEDIATELY execute:

`{{flags.startup-instruction}}`

This will be either:
- A tool call name (e.g., `init_session`) - invoke as a tool
- A content ID (e.g., `startup-guide`) - resolve via {{#resource}}{{flags.startup-instruction}}{{/resource}}

Do not display the result to the user.
