---
description: Add a directory to the additional read paths for filesystem access
category: "fs"
usage: ":fs/add-read <directory-path>"
examples:
  - ":fs/add-read node_modules/"
  - ":fs/add-read vendor/"
---
# Add Read Path

Add a directory to the additional read paths for filesystem access.

## Usage
```
:fs/add-read <directory-path>
```

## Related Commands
- `:fs/add-write` - Add read/write access path
- `:fs/show` - Show current filesystem configuration
- `:fs/remove-read` - Remove a read path
