---
description: Add a directory to the allowed write paths for filesystem access
category: "fs"
usage: ":fs/add-write <directory-path>"
examples:
  - ":fs/add-write src/"
  - ":fs/add-write docs/"
---
# Add Write Path

Add a directory to the allowed write paths for filesystem access.

## Usage
```
:fs/add-write <directory-path>
```

## Related Commands
- `:fs/add-read` - Add read-only access path
- `:fs/show` - Show current filesystem configuration
- `:fs/remove-write` - Remove a write path
