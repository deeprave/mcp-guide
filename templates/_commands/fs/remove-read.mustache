---
description: Remove a directory from the additional read paths for filesystem access
category: "fs"
usage: ":fs/remove-read <directory-path>"
examples:
  - ":fs/remove-read node_modules/"
  - ":fs/remove-read vendor/"
---
# Remove Read Path

Remove a directory from the additional read paths for filesystem access.

## Usage
```
:fs/remove-read <directory-path>
```

## Parameters
- `<directory-path>` - The directory path to remove from read access

## Related Commands
- `:fs/add-read` - Add read-only access path
- `:fs/show` - Show current filesystem configuration
