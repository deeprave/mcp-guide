Check if OpenSpec project is initialized by listing the `openspec/` directory.

Send the directory listing using {{tool_prefix}}send_directory_listing with path `openspec`.

If the directory does not exist, send an empty listing.
