---
type: agent/instruction
description: Check OpenSpec changes directory
---
List the `openspec/changes/` directory and send the listing using {{tool_prefix}}send_directory_listing with path `openspec/changes`.

Exclude hidden files (files starting with `.`) from the listing.
